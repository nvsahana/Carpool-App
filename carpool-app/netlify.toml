[build]
  base = "carpool-app"
  command = "npm run build"
  publish = "build"

[build.environment]
  CI = "false"
# SPA routing - redirect all paths to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200